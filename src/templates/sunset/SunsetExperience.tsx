import type { TemplateExperienceProps } from "../types";
import LuxuryThemedFlow from "../shared/LuxuryThemedFlow";

export default function SunsetExperience(props: TemplateExperienceProps) {
  return <LuxuryThemedFlow {...props} themeId="sunset" />;
}
