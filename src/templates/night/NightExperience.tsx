import type { TemplateExperienceProps } from "../types";
import LuxuryThemedFlow from "../shared/LuxuryThemedFlow";

export default function NightExperience(props: TemplateExperienceProps) {
  return <LuxuryThemedFlow {...props} themeId="night" />;
}
