import type { TemplateExperienceProps } from "../types";
import LuxuryThemedFlow from "../shared/LuxuryThemedFlow";

export default function GardenExperience(props: TemplateExperienceProps) {
  return <LuxuryThemedFlow {...props} themeId="garden" />;
}
