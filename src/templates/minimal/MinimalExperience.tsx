import type { TemplateExperienceProps } from "../types";
import LuxuryThemedFlow from "../shared/LuxuryThemedFlow";

export default function MinimalExperience(props: TemplateExperienceProps) {
  return <LuxuryThemedFlow {...props} themeId="minimal" />;
}
